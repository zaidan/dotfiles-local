export GIT_AUTHOR_NAME="Firas Zaidan"
export GIT_AUTHOR_EMAIL="firas@zaidan.de"
export PATH="${PATH}:${HOME}/bin:${HOME}/.local/bin:/usr/local/bin"

if [ -n "$DISPLAY" ]; then
     BROWSER=chromium
fi
